import React, { useState } from "react";

import {
    Container,
    Button
} from './../../components';

const ViewEdit = () => {
    const [open, setOpen] = useState(false);

    return (
        <p>Hi!</p>
    );
};

export default ViewEdit;